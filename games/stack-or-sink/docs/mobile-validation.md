# Stack or Sink mobile validation — 2026-09-06

Railway deployment `de7eec08-579d-40a2-9912-78e292342958` is successful at https://stack-or-sink-production.up.railway.app/stack-or-sink. The combined release preserves the Jumbleyard collection and Blend Business, including the independently varied herd behavior already deployed by that task.

- TypeScript and the production Railway build pass. All 61 unit/integration tests pass, including four new gesture tests for analog joystick input, dead zones, drag versus tap, pinch scaling, cancellation, extra fingers and interrupted gestures.
- Local and public HTTP suites pass: four-client admission, capacity, authentication, host controls, movement, replay protection, exact preview placement and another client's view of the settled stack. The collection/Blend Business integration suite also passes on the public deployment.
- Browser checks cover 390 × 844 portrait, 360 × 640 portrait, 320 × 568 portrait and 844 × 390 landscape. A 360 × 360 viewport checks a reduced-height join form. Menus scroll on short screens; gameplay stays within its viewport. HUD and action centers are reachable, with no horizontal overflow at the inspected phone sizes.
- Using the visible controls locally and on the public build: move the joystick, select a bathtub, Pick up, tap a crate to aim, Rotate, move closer and Place. The tap keeps the item held. The rotated bathtub settles at x≈0, y≈1.43, z≈3.70 on the selected crate. Joystick input returns to zero after release. The preview remains aimed at the same crate while moving closer.
- Four participants, including long names, were inspected in the small-phone lobby and invite dialog. Start, loss, restart, leave, practice, help and join-error flows were exercised. Help now focuses its heading at scroll position zero. Join errors stay visible while the dialog is open. Production browser error logs are empty after loading and playing.

These are browser viewport checks with mouse-driven pointer controls and gesture unit tests, not a physical iPhone Safari or Android playtest. Native share-sheet behavior, hardware multi-touch and real-device frame rates remain device-testing items. Safe-area padding and visual-viewport keyboard handling are implemented; actual iOS keyboard behavior was not exercised on hardware. This does not claim that every possible gameplay bug has been eliminated.
