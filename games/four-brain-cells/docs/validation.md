# Four Brain Cells validation — 2026-09-08

## NPC slot update

- The lobby and rematch screen support one NPC per selected empty player slot, filling all remaining slots, and removing NPCs. Solo / NPCs opens a local setup lobby. NPCs are labeled in the crew panels and excluded from voice participants.
- Twenty Four Brain Cells tests pass, including complete input-driven breakfasts with a human assigned to each of the four limbs and three NPC teammates. The tests also cover single-slot addition, exact four-seat capacity, removal, host restrictions, human input preservation, fixed limb ownership after switching, round locks, stale roster fencing, peer impersonation rejection, host succession and preserved cookware on recovery.
- Forty-eight focused game, shared peer and Uphill Delivery NPC regression tests pass. The complete shared repository suite passes 733/733 (`.tmp/four-brain-cells-npc-suite.log`). Global TypeScript, scoped lint/format and architecture checks pass.
- The updated Node production build passes (`.tmp/four-brain-cells-npc-build.log`). Its isolated HTTP server on port 3044 passes the page, workshop, artwork, audio manifest, four-member admission, origin/authentication and game isolation checks.
- `node --import tsx games/four-brain-cells/scripts/npc-integration.mjs` passes with real local WebRTC: host adds one NPC, fills the crew, removes one for a human guest, enforces capacity and authority, and the human foot carries an NPC hand to physically pour coffee. NPC identity, served coffee and continued foot movement survive abrupt host loss. The same integration passes against the built server's real `/api/peer` endpoint using `PEER_TEST_URL=http://127.0.0.1:3044`.
- The Windows native WebRTC test binding occasionally produces an invalid first candidate index. This game-owned test harness recovers that index from its known audio/data media IDs; production signal validation remains strict. The temporary production server was stopped after validation. No deployment was performed by this task.

## Original breakfast implementation

- Twelve game tests pass. The complete solo playthrough uses hand/foot inputs to visit the hob, cook both sides of each of three pancakes, carry and serve them, collect the pot, walk to the table, and fill the coffee cup before the timer expires. Other cases cover limb ownership, hostile/duplicate inputs, burning, wrong-location pouring, table kicks, fan collisions, floor replacements, opposing footsteps, disconnect takeover, immutable snapshots and restart.
- Four actual local WebRTC clients pass independent limb movement, exclusive ownership, coordinated walking and a right-foot kick moving the table with attribution. Direct generated voice samples continue across abrupt host loss and graceful host succession; the round is preserved. This check uses the shared peer coordinator and real local WebRTC, not simulated channels.
- Fifty-six focused game, peer, voice, touch-input and shared-narrator tests pass together. Focused TypeScript, lint and formatting checks pass. The full project TypeScript check also passes.
- Live HTTP checks on the existing preview pass for the game page, workshop, illustration and audio manifest, plus four-member admission, room capacity, same-origin enforcement, rejected credentials and cross-game isolation. Test members leave after the check.
- The shared repository suite passes 697/697 after the Tiptoe Thieves task fixed its narrator inheritance assertion. The log is `.tmp/one-more-button-full-suite.log`. Additional Wrong Floor tests were still being added after that run.
- Node/Railway and Worker builds pass for the combined collection. After the last landscape control-layout adjustment, a sequential final Node production build passes again (`.tmp/four-brain-cells-build-node.log`). Architecture boundaries and focused formatting, types and lint pass.
- An isolated production server on port 3044 passes the complete HTTP check and the collection smoke check: all eleven cards, game/admin pages, shared toolbars, workshop destinations, assets, invite redirects, audio namespaces and origin checks. The shared development preview remains available throughout.
- Four real WebRTC clients also pass against that production server's actual `/api/peer` endpoint, including limb ownership and input, shared robot movement, table kicks, voice and both host-recovery paths. The temporary production server was stopped after validation.

The new route is `/four-brain-cells`. The existing local preview is `http://127.0.0.1:3016/four-brain-cells`; its server belongs to the ongoing shared checkout and was left running. This task does not commit or publish the combined checkout. The separate One More Button task received a subsequent user request to publish and was notified that Four Brain Cells is ready for its release snapshot.

The illustration was inspected. No browser interaction/screenshot QA, physical-device test or four-person internet playtest was run. Restrictive networks still require the collection's existing TURN configuration. Immediate synthesized cues are available; paid generation of optional workshop recordings was not requested or run.
