# Four games: complete playable audio libraries

The user approved expanding Four Brain Cells, Reel Problems and Wrong Floor and generating every missing recording for those games plus Brick by Hand. Preserve all existing recorded files and generate the existing 61-cue Brick by Hand catalog.

Use the established ElevenLabs workshops, saved narrator and persistent playback manifests. Add game-specific Foley, movement variations, continuous ambience, phase-dependent music, round feedback and restrained lobby arrival/departure sounds. Every added cue must have an actual runtime trigger. Four Brain Cells follows cooking, utensils and robot movement; Reel Problems follows lines, deck movement, gear, weather and boat motion; Wrong Floor follows its private horror snapshot and keeps audio clues private. Keep the existing synthesized fallback available while a recording is missing. Stop/rebase transient events after stale snapshots, reconnects, round changes and hidden tabs. Lower background layers when friends speak.

Use pure sound directors for the kitchen and lake to separate state comparison from browser playback. Retain Wrong Floor's existing private clue director and add ambience/music and footstep variants. Keep game-state rules and multiplayer semantics unchanged.

Validation: catalog uniqueness/provider limits; state-based trigger and replay tests; no new cue without a runtime path; existing targeted game/audio tests; TypeScript, lint and production build. Publish the exact validated application to its established Jumbleyard deployment and confirm Four Brain Cells' audio namespace. Generate sequentially with idempotent request IDs and resumable progress; verify saved files after each request and avoid replaying uncertain paid calls. Verify all four final manifests and representative decoded media.

Existing authorization covers the audio expansion and generation. Automatic approval review separately requested explicit approval for saved workshop/deployment credential access; the user answered “Yes, use the saved access.” Saved access and publishing are explicitly authorized. No unrelated libraries, access policies, artwork or gameplay changes belong to this pass.

Implementation sequence: add catalogs and directors; connect snapshots and voice ducking; add targeted regressions; validate the application; publish catalogs; generate and verify all missing sounds; save a final count table and validation record. The writing-plans skill was not present in the installed skills directories, so this concrete sequence is retained here.
